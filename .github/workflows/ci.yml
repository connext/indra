name: CI

on:
  push:
    branches-ignore:
      - master

jobs:
  test-bot:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v1
    - run: make dev
    - run: make start-headless
    - run: make test-bot
