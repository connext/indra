To recreate this:
1. Build the contract with `npm run build`
2. Copy `build/ts/ChannelManager.d.ts` here
3. Copy `build/contracts/ChannelManager.json` here, rename it to ChannelManagerAbi.ts, add `export default` on the first line, remove the ABI
