import { ThreadState, ChannelState } from "../../lib/connext-client/types";
import { WalletTimer } from "./WalletTimer";
import { TransactionsState } from "./AtomicTransactionState";

export interface PersistentState {
  didInit: boolean
  keyring?: string
  rememberPath: string
  channel: ChannelState
  threads: ThreadState[]
  walletTimer: WalletTimer|null // WalletTimer for disputing if hub doesn't respond
  lastThreadId: number
  transactions: TransactionsState
}
