import { ProposeMiddlewareContext } from "@connext/types";

export const proposeSwapMiddleware = async (cxt: ProposeMiddlewareContext, addr: string) => {
  throw new Error("proposeSwapMiddleware not implemented");
};
