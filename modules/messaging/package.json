{
  "name": "@connext/messaging",
  "description": "Messaging module for Connext client",
  "version": "7.0.3-alpha.0",
  "main": "dist/index.js",
  "iife": "dist/index.iife.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "rm -rf ./dist/* && tsc -p . && rollup -c",
    "build:watch": "tsc -p . && rollup -c -w",
    "lint": "../../node_modules/.bin/eslint -c '../../.eslintrc.js' --fix 'src/**/*'"
  },
  "dependencies": {
    "@connext/types": "7.0.3-alpha.0",
    "@connext/utils": "7.0.3-alpha.0",
    "ts-natsutil": "1.1.1"
  },
  "devDependencies": {
    "rollup": "2.21.0",
    "rollup-plugin-typescript2": "0.27.1",
    "typescript": "3.9.7"
  }
}
