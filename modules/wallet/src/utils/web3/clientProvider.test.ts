import { expect } from 'chai'
import clientProvider from './clientProvider';

describe('clientProvider', () => {
  it('should write a clientProvider test', () => {
  })
  // really boilerplaty doesn't seem to be a high priority to test
})