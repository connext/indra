/* global before */

// TODO: merge cancel PR!
describe.skip("cancelChallenge", () => {
  it("works", async () => {});

  it("fails if incorrect sigs", async () => {});

  it("fails if is not cancellable", async () => {});

  it("fails if wrong state hash submitted", async () => {});

  it("fails if wrong status submitted", async () => {});
});
